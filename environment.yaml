name: dta-server
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14.3
  - fastapi
  - httpx # ASA inference client (app/utils/asa_client.py)
  - python-dotenv
  - pydantic>=2,<3
platforms: # Specify the platforms you want to support (for lockfile)
  - linux-64
